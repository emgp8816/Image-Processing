

    for x=1:250
        for y=1:300
            if (180<=y && y<=240) && (100<=x && x<=200)
                         
            elseif (y<=70) && ((-6)*y-340<=x && x<=(6)*y+500) 
                
            elseif 150<=x && x<=(200+sqrt((900)-(y-90))^2) && 60<=y && y<=120  
                
            else 
                
            end
        end
    end
    
end
